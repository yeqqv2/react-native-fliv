import React from 'react'
import { StyleSheet, View, Text, Button } from 'react-native';

export const PostDetail = () => {
    return (
        <View>
            <Text>Post Detayı</Text>
        </View>
    )
}

const styles = StyleSheet.create({
    background: {
        backgroundColor: '#333',
        justifyContent: 'center',
        alignItems: 'center',
        flex: 1,
    },
});